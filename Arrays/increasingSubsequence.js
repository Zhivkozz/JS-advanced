"use strict"
function name(paramss) {
  let curr = 0
  for (let i = 0; i < params.length; i++) {
    if (params[i] > curr) { curr = params[i] 
      console.log(curr);}
  }
}
name([1, 
    3, 
    8, 
    4, 
    10, 
    12, 
    3, 
    2, 
    24]
    )