function name(params) {
    let delim = params.pop()
    
    let ss =  params.join(delim)
    // params.forEach(element => { element = element+delim
        
    // });
  console.log(ss);  
}
//100/100
name (['One', 
'Two', 
'Three', 
'Four', 
'Five', 
'-']
);